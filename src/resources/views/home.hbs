<div class="mt-4">
    <div class="row">
        {{#each products}}
        <div class="col-sm-6 col-lg-4 mt-3">
            <div class="card">
                <a href="/product/{{this.slug}}">
                    <img src="{{this.thumb}}" class="card-img-top" alt="...">
                </a>
                <div class="card-body">
                    <a href="/product/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text">
                        {{this.description}}
                    </p>
                    <a href="#" class="btn btn-dark">Xem thêm</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>